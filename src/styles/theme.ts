import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#EC6724', 
      contrastText: '#FFFFFF', 
    },
  },
  typography: {
    fontFamily: '"Inter", sans-serif',
  },
});